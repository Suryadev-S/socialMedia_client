import { Cloudinary } from "@cloudinary/url-gen";

// Create a Cloudinary instance with your Cloudinary cloud name
const cld = new Cloudinary({ cloud: { cloudName: "dnxpue9kq" } });

export default cld;
